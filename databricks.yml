bundle:
  name: customer-medallion

include:
  - resources/*.yml

targets:
  dev:
    default: true
    workspace:
      host: https://<dev-workspace-url>

  prod:
    workspace:
      host: https://<prod-workspace-url>